---
env_name: myaiops-test

account_id: 123456789012

aws_region: us-east-1

k8s_version: "1.35.7"

local_kubeconfig_path: "{{ inventory_dir }}/cluster-admin.conf"

enable_ha: false

resource_tags:
  Project: HungAIOps
  Environment: "{{ env_name }}"
